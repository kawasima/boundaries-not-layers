spring:
  application:
    name: purity-completeness
  datasource:
    url: jdbc:h2:mem:cartdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: always
  jooq:
    sql-dialect: H2
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8081
